Added `mammos_entity.io` version 3. Now `Entity` abd `EntityCollection` instances contain a `description` attribute, containing a string with user-defined information (empty by default). This results in an extra metadata line (in csv format) or key (in yaml format) for version 3 files.
