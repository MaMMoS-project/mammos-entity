Now `Entity` and `EntityCollection` instances have a `description` attribute, containing a string with user-defined information (empty by default). This results in an extra metadata line (in csv format v3) or key (in yaml format v2) for files.
